import React from 'react';

const Admin = () => {
  return (
    <div>
       <h1>Admin Panel</h1> 
      <h1>Admin Panel</h1>
      <p>This is the admin panel for managing the application.</p>
      <p>Click the link below to access Firebase Firestore:</p>
      <a href="https://console.firebase.google.com/project/YOUR_PROJECT_ID/firestore">Firebase Firestore</a>
    </div>
  );
};

export default Admin;
